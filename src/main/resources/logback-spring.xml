<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- KHAI BÁO CÁC CONVERTER MÀU -->
    <conversionRule conversionWord="clr" converterClass="org.springframework.boot.logging.logback.ColorConverter"/>
    <conversionRule conversionWord="faint" converterClass="org.springframework.boot.logging.logback.ColorConverter"/>
    <conversionRule conversionWord="highlight" converterClass="org.springframework.boot.logging.logback.HighlightConverter"/>
    <conversionRule conversionWord="springAppName" converterClass="org.springframework.boot.logging.logback.SpringBootApplicationConverter"/>

    <property name="CONSOLE_LOG_PATTERN"
              value="%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(%5p) %clr([%thread]){cyan} %clr(%logger{36}){blue} : %msg%n"/>

    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="console"/>
    </root>

</configuration>
